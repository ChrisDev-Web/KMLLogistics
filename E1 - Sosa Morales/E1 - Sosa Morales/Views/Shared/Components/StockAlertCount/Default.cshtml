@model int

<a asp-controller="AlertasStock" asp-action="Index"
   class="header-notifications" title="Alertas de stock">
    <i class="bi bi-bell"></i>
    @if (Model > 0)
    {
        <span class="badge">@(Model > 99 ? "99+" : Model.ToString())</span>
    }
</a>
